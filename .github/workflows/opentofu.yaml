name: OpenTofu

on:
  push:
    branches: 
      - '**'
    paths:
      - '**.tf'
      - '**.tfvars'
      - '**.tftpl'

permissions:
  packages: write

jobs:
  validate-tofu:
    name: Validate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          token: ${{ secrets.GH_PAT }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Devcontainer
        uses: devcontainers/ci@v0.3
        with:
          imageName: ghcr.io/${{ github.repository_owner }}/homelab-devcontainer
          cacheFrom: ghcr.io/${{ github.repository_owner }}/homelab-devcontainer
          push: always

      - name: Tofu Init
        uses: devcontainers/ci@v0.3
        with:
          cacheFrom: ghcr.io/${{ github.repository_owner }}/homelab-devcontainer
          push: never
          runCmd: tofu init

      - name: Tofu Validate
        uses: devcontainers/ci@v0.3
        with:
          cacheFrom: ghcr.io/${{ github.repository_owner }}/homelab-devcontainer
          push: never
          runCmd: tofu validate
