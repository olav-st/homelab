apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: traefik
spec:
  parentRefs:
  - name: netbird-private
    namespace: netbird
  rules:
  - backendRefs:
    - name: traefik
      port: 443
