apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: netbird

resources:
  - jobs.yaml
  - netbird-groups.yaml
  - secrets.yaml

helmCharts:
  - name: kubernetes-operator
    releaseName: netbird
    namespace: netbird
    repo: https://netbirdio.github.io/kubernetes-operator
    version: 0.2.2
    includeCRDs: true
    valuesFile: values.yaml

patches:
  - path: patches/fix-nbpolicy-ports.yaml
  - path: patches/fix-missing-envsubst.yaml # Temporary patch until latest tag is updated
